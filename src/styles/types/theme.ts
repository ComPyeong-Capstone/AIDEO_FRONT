// types/theme.ts
export type ThemeColorsType = {
  primary: string;
  primary2?: string;
  maincolor?: string;
  background: string;
  textPrimary: string;
  textSecondary: string;
  secondary: string;
  buttonBackground: string;
  buttonText: string;
  imagebox: string;
  black: string;
  thumbnail: string;          // ✅ 추가
  videocontainer: string;     // ✅ 추가
};
